2016-07-18
1、张志武调节荧光需要添加新的稀释比例，1：40，修改第12比例1：5000为1：3，修改第13比例1：10000为1：4
2、质控异常时没有报出具体的OD值修改
3、保存的数据文件为空数据bug修复
4、添加了配置文件debug选项，表示是否进入后台调控参数。如果是1点击panel_other进入后台，如果是0进入基础维护。

2016-07-21
1、修改bug，开机时会发送查询片仓命令，当收到片仓关闭状态时，会将试剂仓切换到二维码未扫描警示界面。
   更改方法：设置labelqr.text初始值不为空。

2016-07-22
1、发现bug，手动时由于没有样本，而保存的图片带样本号，导致无法手动测试。已经修复

2016-07-25
1、添加识别框识别代码，bitmap转换为int[,]效率较低，修改为bitmapdata模式，可以从160s/226缩小到8s/226。
2、由于反光，截取后的画面依然会有很多黑色区域判别成白色，进行截取后的二次处理
3、bitmapdata的行数为4的整数倍，需调整算法。

2016-08-05
1、打印时弹窗报错（查询语句错误）
2、搜索列表里打印时未传递行号无法打印
(已修复)

2016-08-16
1、添加CMOS校准参数，添加Fluo校准参数及其计算
2、胶体金添加Thit等判断
3、添加片仓光耦校准、液路光耦校准、液路自检、强力清洗、普通清洗模式等操作

2016-08-17
1、添加了命令校验，校验成功结束，校验不成功将重发命令
2、王成陈测试过程中发现荧光前20片测试正常，后面测试有延迟。此处为fluo串口数据日志过多加载缓慢导致。取消fluo日志的保存。

2016-08-08
1、i-Reader S启动时间过长（约6s），采用先启动后用定时器进行加载动作，打开时间减少

2016-08-19
1、命令发出收到ack后主控后续会有出错情形，前端考虑判断收到的命令是否与发出的一致，不一致重发（主控bug仍在需志武找出）
2、可能由于触摸屏的缘故会偶尔出现跳转到其他页面的情形（100ms定时器记录当前页面不一致将会调准）。
3、老主控板无法识别校验，加入配置参数兼容
4、条码识别第一次不成功第二次测试在不正确时间拍摄，修复。
5、条码不能识别限定最多拍2次。

2016-08-23
1、CCD保存图像bmp模式为940kb左右，过大改成jpg格式保存，约为100kb（15.6倍减少）,限定1500行保存一次

2016-08-26
1、质控片测试位置设定为338 638 938 428

2016-08-29
1、片仓空放入1-6片应该显示6片结果显示1、放入7-25显示25显示7的bug处理，取片失败错误导致。

2016-08-30
1、加入制冷片异常情形。

2016-09-01
1、消耗品弹窗只有3秒，用户可能忽视。添加两块颜色交替提示。

2016-09-07
1、添加ASU功能
2、添加了文件及目录判断，防止误删。

2016-09-08
1、荧光校准片取值方法修改为线性计算。
2、荧光测试出错修复---写入日志时格式化问题string.format({1}..{2}),需从{0}开始

2016-09-19
1、PCT本底偏高对取值影响。
2、修改压积功能添加
3、如果主控10s内都无通信内容，将发送#0003，以解决有时候出现通信中断需重新发送命令恢复的状况

2016-09-27
1、主控校验功能
2、主控通信命令排队发送
3、日志千行保存一次

2016-10-11
1、2016-10-11条码识别测试381次，识别出错1次，图像OK，识别算法改进。
   出错分析：白底识别限定200以上，出错样本白底为190，导致识别边界出错。  
   解决办法：限定降低至150
   解决验证：381次测试设定150时全部测试ok。
2、条码识别图片保存格式设定为jpg格式，减小空间使用
3、除湿端口不在控制关闭只控制显示，关机时是否排水不在依靠除湿端口参数。

2016-10-12
1、出错分析：测试中，打开空仓放入25片时，显示为7片，测试过程中需判断片仓状态是由于取片后还是开关片仓后发生的变化。如果是取片过程中需要做-1操作，取片过程是从*3103吸样开始到*3106卡片就绪，测试过程中发生了清洗液不足，发生了两次*3103一次*3106情形，软件误判此刻为取片过程。
   解决办法：以片仓关闭状态来判断是否取片过程，如果片仓状态与片仓关闭在5s内说明是由于关闭片仓带来的变化，否则是取片

2016-10-17
1、片仓打开，提示清理废片仓 提示3s太短，改为messagebox需点击确认。
2、修改日志文件名时出错，更改保存日志方法，每次保存都判断是否存在。
3、每隔4小时发送一次灌注命令

2016-10-25
1、压积公式： 红细胞/（红细胞+血浆）   全血样本系数=1/（1-压积），修改压积输入的是压机比不是调整系数
2、非全血样本不可以修正压积比
3、10-24结果应当显示计算异常，未对错误处理导致图片没保存、结果为卡片移除，添加了错误跳出处理

2016-10-28
1、10-17废片仓打开弹窗会影响主控进程通信传输，影响正常测试（出错内容：片仓打开后未点击确定，此时开始测试命令已经发出但未处理，导致测试时拍到黑底）

2016-11-16
1、更新条码识别算法，应对不规则条码情形，比如条码上侧 下侧 右侧偏出情形

2016-11-22
1、正常测试过程中，扫描了二维码报警提示样本条码不正确修改为扫描的是试剂二维码?请先打开片仓 扫描的是样本条码?请检查条码信息
2、预稀释功能添加

2016-12-28
1、浮球功能板块添加
2、稀释液清洗液容量不可更改
3、光源校准新算法可满足不同型号情形，可满足左右灯珠颠倒情形

2017-01-06
1、结果列表中发送结果功能添加。
2、CRP扩展模式添加
3、项目设置中警示值修改为参考范围

2017-01-16
1、加入片仓加载时间更新
2、片仓打开需要多300ms延迟（!0910片仓打开失败处理,*0112片仓忙 片仓打开）
3、添加片仓禁用后台命令，防止片仓出现的问题，前提是片仓得为空
4、th关闭时，消耗品显示label位置调到中间以美观
5、如果正在更改消耗品容量时，发生液体变化会导致更改出错

v1.02
------------------------------------------------------------------------------------------

2017-01-19
1、当存在i-Reader S进程时，再次打开i-Reader S程序会报端口错误。加入启动时的进程检测功能

2017-01-24
1、保存测试图像时像素点法转为内存法，提示效率，采集计算时间由3s转为1s

2017-02-14
1、软件启动初始化过程中，如果发生错误时，软件将会僵持在倒计时0画面，
   处理后：当倒计时为0时，显示stop按钮，用于软件最小化及关机事宜

2、配置文件异常损坏时，考虑能直接从备份文件进行复制，减少售后幅度。

2017-02-23
1、在测试结果出现计算异常时，将对应照片的文件名记录进数据库。
2、增加功能，在异常结果菜单中点击异常结果样本的任意单元格，即可调取对应异常结果的照片进行显示。

2017-2-24
1、在结果查询界面追加查找结果数量的显示。
2、将已完成的用户管理功能追加进程序。
3、在界面右下角追加操作人员编号的显示。
4、修正片夹的取片顺序，按先放先取的原则来进行片仓的切换。
5、将预稀释模式的“HsCRPD”名称改为“CRPDIL”， 以便更好地表明其意义。

2017-2-25
1、修正按键大小与背景图以避免出现按键文字自动换行的现象。
2、增加读取界面仪器状态的显示。

2017-2-28
1、修正用户管理界面的出现方式。
2、增加开机时仪器状态的读取以及问题的反馈。

2017-3-1
1、能够在固定的时间启动液路自检定标。
2、增加开机机械自检时的仪器状态显示目录。
3、修正用户界面位置及显示。

2017-3-2
1、修改启动界面仪器状态显示项目的设置。